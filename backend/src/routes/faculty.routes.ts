/**
 * routes/faculty.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /faculty/*, /mentor-assignments/*, /class-incharge/*
 */
import { Router } from 'express';
const router = Router();
export default router;
