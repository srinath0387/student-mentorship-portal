/**
 * routes/attendance.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /attendance/allotments/*, /attendance/sessions/*, /attendance/roster/*, etc.
 */
import { Router } from 'express';
const router = Router();
export default router;
