/**
 * routes/proxy.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /proxy/leetcode/:handle, /proxy/github/:handle, /proxy/gfg/:handle, /proxy/eduskills/:handle
 */
import { Router } from 'express';
const router = Router();
export default router;
