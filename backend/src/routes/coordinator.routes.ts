/**
 * routes/coordinator.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /departments, /holidays/*, /academic-calendar/*, /subject-masters/*, /class-incharge/*, /first-year/*
 */
import { Router } from 'express';
const router = Router();
export default router;
