/**
 * routes/admin.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /admin/*, /super-admin/*, /db-migrate
 */
import { Router } from 'express';
const router = Router();
export default router;
