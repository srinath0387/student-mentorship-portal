/**
 * routes/student.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /students/*, /students/:id/*, /students/:id/coding-profiles, etc.
 */
import { Router } from 'express';
const router = Router();
export default router;
