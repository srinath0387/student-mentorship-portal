/**
 * routes/reports.routes.ts
 * Phase 3 stub — handlers migrate here in Phase 4.
 * Routes: /reports/hod-analytics, /reports/department/:dept, /reports/placement-summary
 */
import { Router } from 'express';
const router = Router();
export default router;
